# 이 파일을 .env 로 복사해서 쓴다 (npm run setup 이 자동으로 해준다).
# .env 는 절대 커밋되지 않는다 — .gitignore 에 있다.

# API 키는 A 담당자 한 명만 넣는다.
# 나머지 세 명은 비워둔 채로 `npm run agent:offline` 을 쓰면 된다.
OPENAI_API_KEY=

# 개발용/발표용 모델 전환. 비워두면 agent/config.py 의 기본값을 쓴다.
# KG_MODE=dev
# KG_MODEL_DEV=
# KG_MODEL_DEMO=
